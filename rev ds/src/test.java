import java.util.Scanner;

public class test {
	public static void main(String t[])
	{
		Scanner k = new Scanner(System.in) ;
		int n  ;
		char a='a' , b='b';
		String ch = "ahmed" , ch2 = "ahmed" ;
		
		n = k.nextInt() ;
		a = k.next().charAt(0) ;
		
		Segment s1 = new Segment(new ex_rev( 5.1f ,6f) , new ex_rev(2f , 1f)) ;
		Segment s[] ;
		s = new Segment[n] ;
		for (int i=0 ; i< n ; i++ ) 
		{
			s[i]= new Segment(new ex_rev( (float)(i) ,(float)(2*i)) , new ex_rev((float)(3*i) , 1f)) ;
		}
		
		if (ch.equals(ch2))
		
		if (a==b)  
		
}}

